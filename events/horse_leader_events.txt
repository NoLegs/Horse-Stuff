country_event = {
	id = hrs_horse_leader.101
	is_triggered_only = yes
	hide_window = yes

	trigger = {
		from = {
			OR = {
				species = { is_species_class = HRS } 
				species = { has_trait = trait_pony_power }
			}
			gender = male
		}
	}

	immediate = {
		from = {
			set_is_female = yes
		}
	}
}