##Temporary Test Space (Seperated for my own gain)
namespace = buggo

planet_event = { 
	id = buggo.1
	hide_window = yes
	is_triggered_only = yes
	
	title = OK
	desc = OK

	trigger = {
		FROM = {
			has_trait = trait_oviposition
		}
		FROMFROM = {
			is_robot_pop = no
			NOT = {
				is_same_species = owner_species
			}
		}
		is_controlled_by = FROM
	}
	
	immediate = {
		while = {
			limit = { 
				owner = {
					NOT = { has_technology = tech_chn_egg_incubation }
				}
			}
			count = 1
			create_pop = {
				species = owner_main_species
			}
		}
		
		while = {
			limit = { 
				owner = { has_technology = tech_chn_egg_incubation }
			}
			count = 2
			create_pop = {
				species = owner_main_species
			}
		}
	}
}